
var StartTypB = draw2d.shape.node.Start.extend({

    NAME : "StartTypB",
    
    init : function(attr)
    {
        this._super($.extend({
          width:30,
          height:30,
          bgColor:"#9affd9"
        },attr));
    }
});
